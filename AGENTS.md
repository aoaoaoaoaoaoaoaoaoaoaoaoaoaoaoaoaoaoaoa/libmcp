# Project Instructions

Before meaningful Rust edits, read the [Rust Style Doctrine](/home/main/programming/projects/rust_starter/docs/rust-style-doctrine.md).

The release-channel runtime is optional. Every consumer must remain a normal
standalone MCP executable when managed-release environment variables are absent.
